meta {
  name: Confirm warehouse document
  type: http
  seq: 2
}

post {
  url: {{host}}/api/v1/warehouse/documents/:id/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:id {
  id: document-uuid
}
