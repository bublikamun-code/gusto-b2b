meta {
  name: Cabinet invoice pdf
  type: http
  seq: 8
}

get {
  url: {{host}}/api/v1/cabinet/invoices/:id/pdf
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: invoice-uuid
}
