meta {
  name: Invoice payments
  type: http
  seq: 10
}

get {
  url: {{host}}/api/v1/invoices/:id/payments
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: invoice-uuid
}
