meta {
  name: 2FA enable
  type: http
  seq: 6
}

post {
  url: {{host}}/api/v1/auth/2fa/enable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setVar("totpSecret", res.getBody().data.secret);
  }
}
