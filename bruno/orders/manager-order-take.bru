meta {
  name: Manager order take
  type: http
  seq: 8
}

post {
  url: {{host}}/api/v1/manager/orders/:id/take
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: order-uuid
}
