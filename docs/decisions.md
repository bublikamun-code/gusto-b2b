# Решения (ADR)

Хронология архитектурных решений. Новое решение = новая запись сверху с датой.

## 2026-08-24 — AGENTS.md в корне репозитория

- Входная точка для AI-ассистентов (ZCode/Cursor/Copilot читают его автоматически).
- Не дублирует план: только железные правила и ритуал сессии; при расхождении верен план.
- Кастомные скилы пока не заводим — заведём, когда появится повторяющаяся сложная процедура.

## 2026-08-24 — Стартовый каркас (S01–S04)

- Модульный монолит Spring Boot 3 / Java 21, React 18 + Vite, PostgreSQL 16, Redis 7 (чек-лист v1.1, Часть 1).
- Все зафиксированные решения — в чек-листе, Часть 1.6 (нумерация через sequences, резерв через SELECT FOR UPDATE, доступ к файлам, идемпотентность, BYN/UTC, soft delete).
- Бренд-токены — Часть 1.7, источник — docs/brandbook/.
- Maven Wrapper не используем: сборка локальная/CI через системный mvn, в Docker — образ maven:3.9-eclipse-temurin-21.
- ESLint добавим в S03-расширении; CI пока проверяет frontend сборкой.
